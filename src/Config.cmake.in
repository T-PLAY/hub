
@PACKAGE_INIT@

include ( "${CMAKE_CURRENT_LIST_DIR}/hubTargets.cmake" )

set(CMAKE_CXX_STANDARD @CMAKE_CXX_STANDARD@)
set(CMAKE_CXX_STANDARD_REQUIRED @CMAKE_CXX_STANDARD_REQUIRED@)

if(UNIX)
    find_package(Threads REQUIRED)
endif()

set(HUB_INCLUDE_DIR ${PACKAGE_PREFIX_DIR}/include)


set (HUB_VERSION "@hub_VERSION@")

message(STATUS "Found hub: ${PACKAGE_PREFIX_DIR} (version \"${HUB_VERSION}\")")

# Todo export hub_static target
#if (NOT TARGET hub_static)
#        message(FATAL_ERROR "hub_static is not installed")
#endif()

if (NOT TARGET hub_shared)
        message(FATAL_ERROR "hub_shared is not installed")
endif()

if(TARGET hub_static AND hub_shared)
    add_library(hub ALIAS hub_static)
else()
    add_library(hub ALIAS hub_shared)
endif()
